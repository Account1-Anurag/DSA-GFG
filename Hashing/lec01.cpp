#include <bits/stdc++.h>
using namespace std;

int main(){
    stack<int>s;
    s.push(10);
    s.push(20);
    s.push(119);
    s.find(10);
    s.find(20);
    s.pop();
    s.pop();
    s.push(119);
    s.find()
    return 0;
}